spring:
  config:
    import: application-common.yml
  datasource:
    url: jdbc:mysql://localhost:33060/techlog?useSSL=false&allowPublicKeyRetrieval=true
    username: user
    password: user
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
